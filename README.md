# Lexicode landing page

The marketing page for Lexicode, served as a static site from this repository's root.

- `index.html` — the whole page (styles inline)
- `assets/` — the two product screenshots
